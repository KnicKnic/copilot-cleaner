<Window x:Class="CopilotCleaner.MainWindow"
        xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:CompileBindings="False"
        Title="Copilot Cleaner"
        Height="760"
        MinHeight="620"
        Width="1240"
        MinWidth="960"
        FontSize="11"
        Background="#F5F7FA">
    <Window.Styles>
        <Style Selector="DataGrid">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="RowHeight" Value="17" />
            <Setter Property="ColumnHeaderHeight" Value="20" />
        </Style>
        <Style Selector="DataGridRow">
            <Setter Property="MinHeight" Value="20" />
        </Style>
        <Style Selector="DataGridCell">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Padding" Value="1,0" />
        </Style>
        <Style Selector="DataGridColumnHeader">
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="MinHeight" Value="22" />
        </Style>
        <Style Selector="Button">
            <Setter Property="Padding" Value="9,4" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="MinWidth" Value="68" />
        </Style>
        <Style Selector="TextBox">
            <Setter Property="Padding" Value="6,3" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style Selector="ComboBox">
            <Setter Property="Padding" Value="6,3" />
            <Setter Property="MinWidth" Value="180" />
        </Style>
    </Window.Styles>

    <Grid Margin="12" RowDefinitions="*,Auto">
        <TabControl Grid.Row="0">
            <TabItem Header="Session State">
                <Grid Margin="0,8,0,0" RowDefinitions="Auto,Auto,*,Auto,200">
                    <Border Grid.Row="0" Background="White" BorderBrush="#DDE3EA" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <Grid ColumnDefinitions="Auto,*,Auto,Auto" RowDefinitions="Auto,8,Auto">
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Session state" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0" />
                            <TextBox x:Name="SourcePathTextBox" Grid.Row="0" Grid.Column="1" />
                            <Button Grid.Row="0" Grid.Column="2" Content="Browse" Click="BrowseSource_Click" Margin="8,0,8,0" />
                            <StackPanel Grid.Row="0" Grid.Column="3" Orientation="Horizontal">
                                <Button x:Name="ScanButton" Content="Scan" Click="Scan_Click" FontWeight="SemiBold" />
                                <ProgressBar x:Name="ScanProgressBar" Width="90" Height="8" IsIndeterminate="True" IsVisible="False" VerticalAlignment="Center" Margin="0,0,8,0" />
                            </StackPanel>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Move target" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0" />
                            <TextBox x:Name="DestinationPathTextBox" Grid.Row="2" Grid.Column="1" />
                            <Button Grid.Row="2" Grid.Column="2" Content="Browse" Click="BrowseDestination_Click" Margin="8,0,8,0" />
                            <StackPanel Grid.Row="2" Grid.Column="3" Orientation="Horizontal">
                                <Button Content="Move" Click="MoveSelected_Click" />
                                <Button Content="Delete" Click="DeleteSelected_Click" Background="#B42318" Foreground="White" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Row="1" Background="White" BorderBrush="#DDE3EA" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
                        <DockPanel LastChildFill="False">
                            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                                <CheckBox x:Name="SelectAllCheckBox" Content="Select all visible" VerticalAlignment="Center" Margin="0,0,16,0" IsCheckedChanged="SelectAllVisible_Changed" />
                                <TextBlock Text="Aggregate by" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0" />
                                <ComboBox x:Name="AggregateColumnComboBox" />
                                <Button Content="Add level" Click="Aggregate_Click" Margin="8,0,8,0" />
                                <Button Content="Up" Click="MoveAggregateUp_Click" />
                                <Button Content="Down" Click="MoveAggregateDown_Click" />
                                <Button Content="Remove" Click="RemoveAggregate_Click" />
                                <Button Content="Clear" Click="ClearAggregate_Click" />
                                <ListBox x:Name="ActiveAggregateListBox" Width="320" Height="34" Margin="8,0,0,0" />
                            </StackPanel>
                            <TextBlock x:Name="SummaryTextBlock" DockPanel.Dock="Right" VerticalAlignment="Center" Foreground="#526173" />
                        </DockPanel>
                    </Border>

                    <DataGrid x:Name="SessionsGrid"
                              Grid.Row="2"
                              AutoGenerateColumns="False"
                              CanUserReorderColumns="True"
                              CanUserResizeColumns="True"
                              CanUserSortColumns="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              IsReadOnly="False"
                              ItemsSource="{Binding SessionView}"
                              MinColumnWidth="40"
                              SelectionMode="Extended"
                              KeyDown="SessionsGrid_KeyDown"
                              Sorting="SessionsGrid_Sorting"
                              ColumnReordered="SessionsGrid_ColumnReordered"
                              SelectionChanged="SessionsGrid_SelectionChanged"
                              Background="White"
                              BorderBrush="#DDE3EA"
                              BorderThickness="1" />

                    <GridSplitter Grid.Row="3" Height="6" HorizontalAlignment="Stretch" Background="Transparent" />

                    <Border Grid.Row="4" Background="White" BorderBrush="#DDE3EA" BorderThickness="1" CornerRadius="6" Margin="0,6,0,0">
                        <TabControl BorderThickness="0">
                            <TabItem Header="Files">
                                <Grid RowDefinitions="Auto,*">
                                    <DockPanel Grid.Row="0" Margin="8,5,8,4">
                                        <TextBlock Text="File list" FontWeight="SemiBold" VerticalAlignment="Center" />
                                        <TextBlock x:Name="FileListSummaryTextBlock" DockPanel.Dock="Right" Foreground="#526173" VerticalAlignment="Center" />
                                    </DockPanel>
                                    <DataGrid x:Name="FileListGrid"
                                              Grid.Row="1"
                                              AutoGenerateColumns="False"
                                              CanUserReorderColumns="True"
                                              CanUserResizeColumns="True"
                                              CanUserSortColumns="True"
                                              GridLinesVisibility="Horizontal"
                                              HeadersVisibility="Column"
                                              IsReadOnly="True"
                                              MinColumnWidth="40"
                                              BorderThickness="0">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" SortMemberPath="Kind" Width="80" MinWidth="50" />
                                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" SortMemberPath="Name" Width="180" MinWidth="70" />
                                            <DataGridTextColumn Header="Relative path" Binding="{Binding RelativePath}" SortMemberPath="RelativePath" Width="420" MinWidth="120" />
                                            <DataGridTextColumn Header="Size" Binding="{Binding Size}" SortMemberPath="SizeBytes" Width="100" MinWidth="60" />
                                            <DataGridTextColumn Header="Last modified" Binding="{Binding LastModified, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" SortMemberPath="LastModified" Width="160" MinWidth="100" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>
                            </TabItem>
                            <TabItem Header="Metadata">
                                <Grid RowDefinitions="Auto,*">
                                    <DockPanel Grid.Row="0" Margin="8,5,8,4">
                                        <TextBlock Text="Metadata values" FontWeight="SemiBold" VerticalAlignment="Center" />
                                        <TextBlock x:Name="MetadataSummaryTextBlock" DockPanel.Dock="Right" Foreground="#526173" VerticalAlignment="Center" />
                                    </DockPanel>
                                    <DataGrid x:Name="MetadataGrid"
                                              Grid.Row="1"
                                              AutoGenerateColumns="False"
                                              CanUserResizeColumns="True"
                                              CanUserSortColumns="True"
                                              GridLinesVisibility="Horizontal"
                                              HeadersVisibility="Column"
                                              IsReadOnly="True"
                                              MinColumnWidth="40"
                                              BorderThickness="0">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Key" Binding="{Binding Key}" SortMemberPath="Key" Width="280" MinWidth="100" />
                                            <DataGridTextColumn Header="Value" Binding="{Binding Value}" SortMemberPath="Value" Width="520" MinWidth="120" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>
                            </TabItem>
                        </TabControl>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Copilot SDK Sessions">
                <Grid Margin="0,8,0,0" RowDefinitions="Auto,Auto,*">
                    <Border Grid.Row="0" Background="White" BorderBrush="#DDE3EA" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                        <Grid ColumnDefinitions="Auto,*,Auto,Auto">
                            <TextBlock Grid.Column="0" Text="Copilot home" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0" />
                            <TextBox x:Name="CopilotHomePathTextBox" Grid.Column="1" />
                            <Button Grid.Column="2" Content="Browse" Click="BrowseCopilotHome_Click" Margin="8,0,8,0" />
                            <StackPanel Grid.Column="3" Orientation="Horizontal">
                                <Button x:Name="LoadCopilotSdkSessionsButton" Content="Load" Click="LoadCopilotSdkSessions_Click" FontWeight="SemiBold" />
                                <ProgressBar x:Name="CopilotSdkProgressBar" Width="90" Height="8" IsIndeterminate="True" IsVisible="False" VerticalAlignment="Center" Margin="0,0,8,0" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Row="1" Background="White" BorderBrush="#DDE3EA" BorderThickness="1" CornerRadius="6" Padding="8" Margin="0,0,0,8">
                        <DockPanel LastChildFill="False">
                            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="Select missing state" Click="SelectSdkSessionsWithoutState_Click" MinWidth="140" />
                                <Button Content="Clear selection" Click="ClearCopilotSdkSelection_Click" MinWidth="130" />
                                <Button x:Name="DeleteSelectedCopilotSdkSessionsButton" Content="Delete selected" Click="DeleteSelectedCopilotSdkSessions_Click" Background="#B42318" Foreground="White" MinWidth="130" />
                            </StackPanel>
                            <TextBlock x:Name="CopilotSdkSummaryTextBlock" DockPanel.Dock="Right" VerticalAlignment="Center" Foreground="#526173" />
                        </DockPanel>
                    </Border>

                    <DataGrid x:Name="CopilotSdkSessionsGrid"
                              Grid.Row="2"
                              AutoGenerateColumns="False"
                              CanUserReorderColumns="True"
                              CanUserResizeColumns="True"
                              CanUserSortColumns="True"
                              Sorting="CopilotSdkSessionsGrid_Sorting"
                              ColumnReordered="CopilotSdkSessionsGrid_ColumnReordered"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              IsReadOnly="False"
                              ItemsSource="{Binding CopilotSdkSessions}"
                              MinColumnWidth="40"
                              SelectionMode="Extended"
                              KeyDown="CopilotSdkSessionsGrid_KeyDown"
                              Background="White"
                              BorderBrush="#DDE3EA"
                              BorderThickness="1">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="" Binding="{Binding IsSelected, Mode=TwoWay}" Width="42" CanUserReorder="False" CanUserSort="False" />
                            <DataGridTextColumn Header="Has session-state" Binding="{Binding SessionState}" SortMemberPath="HasSessionState" Width="135" MinWidth="90" IsReadOnly="True" />
                            <DataGridTextColumn Header="Modified" Binding="{Binding ModifiedTime, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" SortMemberPath="ModifiedTime" Width="170" MinWidth="120" IsReadOnly="True" />
                            <DataGridTextColumn Header="workspace.cwd" Binding="{Binding WorkingDirectory}" SortMemberPath="WorkingDirectory" Width="280" MinWidth="140" IsReadOnly="True" />
                            <DataGridTextColumn Header="workspace.git_root" Binding="{Binding GitRoot}" SortMemberPath="GitRoot" Width="280" MinWidth="140" IsReadOnly="True" />
                            <DataGridTextColumn Header="workspace.branch" Binding="{Binding Branch}" SortMemberPath="Branch" Width="180" MinWidth="100" IsReadOnly="True" />
                            <DataGridTextColumn Header="Summary" Binding="{Binding Summary}" SortMemberPath="Summary" Width="360" MinWidth="160" IsReadOnly="True" />
                            <DataGridTextColumn Header="Remote" Binding="{Binding Remote}" SortMemberPath="IsRemote" Width="90" MinWidth="70" IsReadOnly="True" />
                            <DataGridTextColumn Header="Started" Binding="{Binding StartTime, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" SortMemberPath="StartTime" Width="170" MinWidth="120" IsReadOnly="True" />
                            <DataGridTextColumn Header="Session ID" Binding="{Binding SessionId}" SortMemberPath="SessionId" Width="280" MinWidth="160" IsReadOnly="True" />
                            <DataGridTextColumn Header="Repository" Binding="{Binding Repository}" SortMemberPath="Repository" Width="280" MinWidth="140" IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

    <Border Grid.Row="1" Margin="0,8,0,0" Background="Transparent">
            <TextBlock x:Name="StatusTextBlock" Foreground="#526173" />
        </Border>
    </Grid>
</Window>
